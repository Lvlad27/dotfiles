alias ll='exa -l --icons -a'

# +-----+
# | Git |
# +-----+

alias ga='git add'
alias gc='git commit'
alias gp='git push'

# +----+
# | cp |
# +----+

alias cp='cp -v'
alias mv='mv -v'
alias rm='rm -v'

# +------+
# | tmux |
# +------+

alias tmn='tmux new-session'

# +-----+
# | apt |
# +-----+
alias ai='sudo apt-get install -y'
alias au='sudo apt-get update && sudo apt-get upgrade -y'
alias aup='sudo apt-get update'
alias aug='sudo apt-get upgrade -y'

# +--------+
# | neovim |
# +--------+

alias v='nvim'
alias v.='nvim .'

# +----------+
# | Projects |
# +----------+

alias proj='z Projects'
alias npm-fm='export CODEARTIFACT_AUTH_TOKEN=`aws codeartifact get-authorization-token --domain fm --domain-owner 264190062813 --region ap-southeast-2 --query authorizationToken --output text`'

